fun main (args: Array<String>){

    /*var numeroEscolhido = (1..10).random()


    println("Entre com um numero ?")
    var numeroDigi = readLine()?.toInt()
    var tentativas = 0;

    while (numeroDigi != numeroEscolhido ){
        if (numeroDigi != null) {
            if (numeroDigi < numeroEscolhido){
                println("Numero Maior \n Entre com um numero ? ")
                numeroDigi = readLine()?.toInt()
                tentativas ++
            }else if (numeroDigi > numeroEscolhido){
                println("Numero Menor \n Entre com um numero ? ")
                numeroDigi = readLine()?.toInt()
                tentativas ++
            }
        }

    }
    println("Voce Ganhou  $numeroEscolhido com $tentativas tentativas ")
    */



}
